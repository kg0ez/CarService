@model IEnumerable<CarService.Common.ModelDto.PurchaseHistoryDto>

@*<head>
    <link rel="stylesheet" href="~/css/History.css" asp-append-version="true" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;200;300;400;500;600;700;800;900&family=Poppins:wght@500;700&display=swap" rel="stylesheet">
</head>*@
<body>
    <div class="wrapper">
        <div class="page__main-block main-block">
            <div align="center" class="main-block__container _container">
                <h1 class="main-block__title" align="center">История платежей</h1>
                @foreach (var ph in Model)
                {
                    <div class="main-block__body2">
                        <div class="body2-left">

                            <img class="main__img" src="~/img/@ph.Img" alt="img" />
                        </div>
                        <div>

                            <div class="title-couple main-couple">
                                <div align="left" class="form__title-user">@ph.Name</div>
                                <form method="post" asp-action="Trash" asp-controller="Home">
                                    <input class="form__input-card" name="historyId" value="@ph.Id" hidden="hidden" />
                                    <input class="form__input-trash" type="submit" value="" />
                                </form>
                            </div>
                            <div class="title-couple">
                                <div align="left" class="form__subtitle">Сумма к оплате: </div>
                                <div align="left" class="form__title"> @ph.Price руб</div>
                            </div>
                            <div class="title-couple">
                                <div align="left" class="form__subtitle">Время продажи:</div>
                                <div class="form__title title-time" align="right"> @ph.PurchaseTime</div>
                            </div>
                        </div>

                    </div>
                }
            </div>
        </div>
    </div>
</body>

